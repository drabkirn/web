<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= @meta_title ? @meta_title : "Drabkirn - Create the world out of the things that you build" %> | Drabkirn
    </title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="author" content="drabkirn">
    <meta name="copyright" content="drabkirn, 2019-2020">
    <meta name="robots" content="index,follow">

    <meta name="description" content="<%= @meta_description ? @meta_description : 'Create the world out of the things that you build. We build those things and will set them free. We also write thoughts and much more.' %>" />
    <meta name="keywords" content="<%= @meta_keywords ? @meta_keywords : 'drabkirn, privacy, desityle, quotes, quotes api, authna, docs, feedka, cdadityang' %>" />

    <!-- Facebook Meta -->
    <% default_app_url = "https://drabkirn.cdadityang.xyz" %>
    <meta property="og:url" content="<%= @meta_app_url ? @meta_app_url : default_app_url %>" />
    <meta property="og:image" content="<%= @meta_og_image ? @meta_og_image : asset_url('og_images/drabkirn.png') %>" />
    <meta property="og:description" content="<%= @meta_description ? @meta_description : 'Create the world out of the things that you build. We build those things and will set them free. We also write thoughts and much more.' %>" />
    <meta property="og:title" content="<%= @meta_title ? @meta_title : 'Drabkirn - Create the world out of the things that you build' %>" />
    <meta property="og:site_name" content="<%= @meta_site_name ? @meta_site_name : 'Drabkirn' %>" />
    <meta property="og:see_also" content="<%= default_app_url %>" />

    <!-- G+ Meta tags -->
    <meta itemprop="name" content="<%= @meta_title ? @meta_title : 'Drabkirn - Create the world out of the things that you build' %>" />
    <meta itemprop="description" content="<%= @meta_description ? @meta_description : 'Create the world out of the things that you build. We build those things and will set them free. We also write thoughts and much more.' %>" />
    <meta itemprop="image" content="<%= @meta_og_image ? @meta_og_image : asset_url('og_images/drabkirn.png') %>" />

    <!-- Twitter Meta Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@drabkirn" />
    <meta name="twitter:creator" content="@drabkirn" />
    <meta name="twitter:url" content="<%= @meta_app_url ? @meta_app_url : default_app_url %>" />
    <meta name="twitter:title" content="<%= @meta_title ? @meta_title : 'Drabkirn - Create the world out of the things that you build' %>" />
    <meta name="twitter:description" content="<%= @meta_description ? @meta_description : 'Create the world out of the things that you build. We build those things and will set them free. We also write thoughts and much more.' %>" />
    <meta name="twitter:image" content="<%= @meta_og_image ? @meta_og_image : asset_url('og_images/drabkirn.png') %>" />

    <!-- Themes Meta -->
    <meta name="theme-color" content="#2A2D31" />
    <meta name="msapplication-navbutton-color" content="#2A2D31" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="#2A2D31" />

    <!-- Icons Meta -->
    <%= favicon_link_tag 'drabkirn-logo-180x180.png', rel: 'apple-touch-icon', type: 'image/png', sizes: "180x180" %>
    <%= favicon_link_tag 'icons/favicon-32x32.png', type: 'image/png', sizes: "32x32" %>
    <%= favicon_link_tag 'icons/favicon-16x16.png', type: 'image/png', sizes: "16x16" %>

    <!-- Manifest -->
    <link rel="manifest" href="/manifest.json">

    <!-- Website verifications -->
    <meta name="yandex-verification" content="fc2cf54c0571aac8" />
    <meta name="msvalidate.01" content="7506120C6AA700DBA4344E736491AED6" />

    <%= javascript_pack_tag 'index', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'index', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <noscript>
      <p><img src="https://analytics.cdadityang.xyz/matomo.php?idsite=3&amp;rec=1" style="border:0;" alt="" /></p>
      <p>You need to enable JavaScript to run this app.</p>
    </noscript>

    <% if show_navigation_array.include? "#{params[:controller]}/#{params[:action]}" %>
      <%= render 'navigation' %>
    <% end %>
    
    <%= render 'flash' %>

    <%= yield %>

    <%= debug(params) if Rails.env.development? %>

    <%= render 'analytics' if Rails.env.production? %>
  </body>
</html>
